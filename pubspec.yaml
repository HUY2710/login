name: flutter_base
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  flutter_bloc: ^8.1.3
  get_it: ^7.6.0
  injectable: ^2.3.0
  shared_preferences: ^2.2.0
  flutter_svg: ^2.0.7
  flutter_dotenv: ^5.1.0
  firebase_remote_config: ^4.2.5
  firebase_analytics: ^10.4.5

  # Route
  auto_route: ^7.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  injectable_generator: 
  auto_route_generator:
  flutter_lints: ^2.0.2
  flutter_gen_runner: ^5.3.1

  # Flavor
  flutter_flavorizr: ^2.2.1

flutter:
  generate: true
  uses-material-design: true

  assets:
   - assets/images/

  fonts:
   - family: OpenSans
     fonts:
       - asset: assets/fonts/open_sans/OpenSans-Regular.ttf

#generate
flutter_gen:
  output: lib/gen/ # Optional (default: lib/gen/)
  line_length: 80 # Optional (default: 80)

  integrations:
    flutter_svg: true

  colors:
    inputs:
      - assets/color/colors.xml
    outputs:
      class_name: MyColors

  assets:
    outputs:
      class_name: MyAssets


#flavor
flavorizr:
  flavors:
    dev:
      app:
        name: "Example Dev"

      android:
        applicationId: "com.vtn.example.dev"
        # icon: "assets/images/logo.png"
        # firebase:
        #   config: "android/app/src/dev/google-services.json"
      ios:
        bundleId: "com.vtn.example.dev"
        # icon: "assets/images/logo.png"
        # firebase:
        #   config: "ios/Runner/GoogleService-Info-Dev.plist"

    prod:
      app:
        name: "Example"
      android:
        applicationId: "com.vtn.example.prod"
        # icon: "assets/images/logo_app.png"
        # firebase:
        #   config: "android/app/src/prod/google-services.json"
      ios:
        bundleId: "com.vtn.example.prod"
        # icon: "assets/images/logo_app.png"
        # firebase:
        #   config: "ios/Runner/GoogleService-Info-Prod.plist"